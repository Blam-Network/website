import { halo3GamesPageMetadata } from "@/src/utils/sectionMetadata";

export const metadata = halo3GamesPageMetadata;

export default function Halo3GamesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
