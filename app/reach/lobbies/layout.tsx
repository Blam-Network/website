import { reachLobbiesPageMetadata } from "@/src/utils/sectionMetadata";

export const metadata = reachLobbiesPageMetadata;

export default function ReachLobbiesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
